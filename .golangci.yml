version: "2"

run:
  timeout: 5m
  go: "1.26"

linters:
  enable:
    - govet
    - staticcheck
    - errcheck
    - ineffassign
    - unused
    - misspell

issues:
  exclude-rules:
    - path: "_test\\.go"
      linters:
        - errcheck
